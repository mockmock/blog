checkout:
  post:
    - git submodule sync
    - git submodule update --init
dependencies:
  pre:
    - go get -v github.com/spf13/hugo
    - hugo version
deployment:
  master:
    branch: master
    commands:
      - ./deploy.sh
